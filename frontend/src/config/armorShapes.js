/**
 * 光衣的幾何 —— **圖形的單一來源**。
 *
 * 原本是 `Armor.jsx` 裡 14 段幾乎相同的 JSX：每個部位都要自己寫一次
 * `<rect>`、一次選取高亮用的 `<rect fill="none">`、一次 onClick。兩份座標
 * 各寫一遍的結果是它們會不一致——鞋子的高亮框寫 `height=25`，實際圖形只有
 * 15，選起來的框比鞋子高一截。抽成表之後圖形與高亮吃同一組數字，不可能對不上。
 *
 * ## 座標系
 *
 * 一個部位可以由多個形狀組成（帽子 = 帽冠 + 帽簷，領帶 = 長方形 + 三角形），
 * 所以 `shapes` 是陣列。索引就是 `constants/parts.js` 的身體部位索引，
 * 新增部位時在這裡補一列即可，`partsConfig.test.js` 會檢查有沒有漏。
 *
 * ## 圓角是算出來的，不是逐個填的
 *
 * `rx = min(寬, 高) × 0.3`，上限 8。逐個手填的話遲早會出現「這塊 6 那塊 7」
 * 的漂移，而那種不一致正是畫面看起來廉價的原因。細長的鞋子與帽簷因此自動
 * 拿到比較小的圓角，粗的部位一律 8——比例一致，看起來才像同一套設計。
 */

const MAX_RADIUS = 8;

/** 依形狀的短邊決定圓角，讓細長的部位不會被圓角吃掉 */
export const radiusOf = ({ width, height }) =>
  Math.min(MAX_RADIUS, Math.round(Math.min(width, height) * 0.3 * 10) / 10);

const rect = (x, y, width, height) => ({ kind: "rect", x, y, width, height });
const circle = (cx, cy, r) => ({ kind: "circle", cx, cy, r });
const polygon = (points) => ({ kind: "polygon", points });

/**
 * 每個身體部位由哪些形狀組成。索引 = `PARTS` 裡身體部位的順序。
 *
 * 版面是一個 5px 的間隙網格：手臂 35..67、胸 72..100、領帶 105..137、
 * 胸 142..170、手臂 175..207；縱向 5..38（帽）、103..168（上半身）、
 * 173..208（腰與手套）、213..293（腿）、298..313（鞋）。動座標時保持這個
 * 網格，不然部位之間的縫隙會寬窄不一。
 */
export const ARMOR_SHAPES = [
  // 0 hat —— 帽冠與帽簷分開畫。原本是一個有五個直角的多邊形，在一堆圓角
  // 方塊中間特別突兀；拆成兩個圓角矩形之後跟其他部位是同一套語言。
  [rect(96.8, 5, 48.4, 18), rect(72.6, 23, 96.8, 15)],
  [circle(121, 68, 30)], // 1 face
  [rect(72, 103, 28, 65)], // 2 chestL
  [rect(142, 103, 28, 65)], // 3 chestR
  [rect(35, 103, 32, 65)], // 4 armL
  [rect(175, 103, 32, 65)], // 5 armR
  [rect(105, 103, 32, 50), polygon("105,153 137,153 121,173")], // 6 tie
  [rect(78, 173, 86, 35)], // 7 belt
  [rect(35, 173, 32, 35)], // 8 gloveL
  [rect(175, 173, 32, 35)], // 9 gloveR
  [rect(85, 213, 28, 80)], // 10 legL
  [rect(129, 213, 28, 80)], // 11 legR
  [rect(75, 298, 45, 15)], // 12 shoeL
  [rect(122, 298, 45, 15)], // 13 shoeR
];

/**
 * 圖形的實際範圍加一點留白。
 *
 * 舊版是 `viewBox="10 0 222 480"`，但內容只畫到 y=348——**底下空了 130px**，
 * 於是等比縮放進卡片之後圖形只佔到上面七成，看起來又小又偏上。收緊之後同一張
 * 卡片裡的光衣大了三成，而且是置中的。
 */
export const ARMOR_VIEWBOX = "27 -3 188 324";

/**
 * 舞台地板線：鞋底再往下 5px，左右與手臂外緣切齊。
 *
 * 對齊手臂而不是隨便畫寬一點，是為了讓這條線看起來是圖形的一部分，
 * 而不是後來補上去的裝飾。
 */
export const ARMOR_FLOOR = { y: 318, x1: 35, x2: 207 };
